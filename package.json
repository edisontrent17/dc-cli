{
  "name": "dc-cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for Salesforce Data 360 connectors, connections, and data streams.",
  "type": "module",
  "bin": {
    "dc-cli": "./bin/dc-cli"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --loader ts-node/esm src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  }
}
